builder:
  display-name: "<blue>Builder"
  description: "Builds structures and earns from placing blocks."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

lumberjack:
  display-name: "<green>Lumberjack"
  description: "Chops trees and earns from breaking logs."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

miner:
  display-name: "<gray>Miner"
  description: "Mines ores and stones deep underground."
  max-level: 100
  upgrade-level: 30
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)
  # Universal perks (all players get these)
  perk-unlocks:
    5: [lavaimmunity]
    10: [stoneskin]
    15: [fargather, storage.tworows]
    20: [netherlavaimmunity]
    25: [obsidianskin]
    30: [storage.threerows]

farmer:
  display-name: "<yellow>Farmer"
  description: "Harvests crops and tends animals."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

hunter:
  display-name: "<red>Hunter"
  description: "Kills mobs and earns from combat."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

fisherman:
  display-name: "<aqua>Fisherman"
  description: "Fishes in lakes, rivers, and oceans."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

blacksmith:
  display-name: "<dark_gray>Blacksmith"
  description: "Smelts ores and crafts gear."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

enchanter:
  display-name: "<light_purple>Enchanter"
  description: "Earns from enchanting and repairing items."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

alchemist:
  display-name: "<gold>Alchemist"
  description: "Brews potions and combines ingredients."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

# --- AzothMC §8.1 tracks not covered by legacy keys above ---
# (mining→miner, woodcutting→lumberjack, farming→farmer, fishing→fisherman,
#  alchemy→alchemist, weaponsmithing→blacksmith via ProfessionCatalog)

herbalism:
  display-name: "<dark_green>Herbalism"
  description: "Gathers herbs and plants for alchemy and cooking."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

smelting:
  display-name: "<gray>Smelting"
  description: "Processes ores into refined metals."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

milling:
  display-name: "<yellow>Milling"
  description: "Mills grains and plant fibers into useful materials."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

tanning:
  display-name: "<gold>Tanning"
  description: "Turns hides into leather and treated materials."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

refining:
  display-name: "<aqua>Refining"
  description: "Refines raw goods into higher-tier crafting inputs."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

cooking:
  display-name: "<red>Cooking"
  description: "Prepares food and consumable buffs."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

armorsmithing:
  display-name: "<dark_aqua>Armorsmithing"
  description: "Forges armor and defensive gear."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

tailoring:
  display-name: "<light_purple>Tailoring"
  description: "Sews cloth and light armor from fibers and hides."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)

engineering:
  display-name: "<dark_purple>Engineering"
  description: "Builds tools, gadgets, and mechanical components."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)


artisan:
  display-name: "<dark_purple>Artisan"
  description: "Crafts, bakes, and creates useful goods."
  max-level: 100
  leveling-curve: level + 4 * level + level * level * level * 10
  payable-curves:
    experience: base + (base * (level - 1) * 0.01)
    economy: base + (base * (level - 1) * 0.01)